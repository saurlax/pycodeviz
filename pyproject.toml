[project]
name = "pycodeviz"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "graphviz>=0.20.0",
    "networkx>=3.0",
]

[project.scripts]
pycodeviz = "pycodeviz.main:main"

[tool.uv]
package = true
