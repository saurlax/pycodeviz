[project]
name = "pycodeviz"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
pycodeviz = "pycodeviz.main:main"

[tool.uv]
package = true
